import type { Lesson } from './curriculum/types';
export function lessonMarkdown(l:Lesson){return `# ${l.title}\n\n${l.summary}\n\n## Intuition\n\n${l.intuition.join('\n\n')}\n\n## How it works\n\n${l.steps.map((s,i)=>`${i+1}. ${s}`).join('\n')}\n\n## Mathematics\n\n$$\n${l.formula}\n$$\n\n${l.symbols}\n\n## ${l.example.title}\n\n${l.example.body}\n\n${l.example.steps.map((s,i)=>`${i+1}. ${s}`).join('\n')}\n\n## Common pitfalls\n\n${l.pitfalls.map(s=>`- ${s}`).join('\n')}\n\n## Python example\n\n\`\`\`python\n${l.code}\n\`\`\`\n\n## Practice\n\n${l.exercise}\n\n### Worked answer\n\n${l.solution}\n\n## Further reading\n\n${l.sources.map(s=>`- [${s.title}](${s.url})`).join('\n')}\n\nOriginal ML Atlas notes. CC BY 4.0. Third-party references retain their own licenses.\n`;}
