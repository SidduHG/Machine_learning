import type { Metadata } from 'next';
import { lessons } from '@/lib/curriculum';
export const metadata:Metadata={title:'Resources, sources & privacy'};
export default function Resources(){const sources=Array.from(new Map(lessons.flatMap(l=>l.sources).map(s=>[s.url,s])).values());return <main id="main" className="wrap section resources"><div className="page-head"><div className="eyebrow">KEEP EXPLORING</div><h1>Built on curiosity.<br/>Grounded in good sources.</h1><p>ML Atlas contains original explanations and small educational examples. These primary references offer more depth, production APIs, and the research behind the ideas.</p></div><div className="resource-grid">{sources.map(s=><a className="panel" href={s.url} target="_blank" rel="noreferrer" key={s.url}><h2>{s.title} ↗</h2><p>{new URL(s.url).hostname}</p></a>)}</div><section className="section prose"><h2>About the course</h2><p>This is a broad practical learning path, not an exhaustive catalog of every ML research topic or a guarantee of employment. Study times are estimates including the worked exercise. Numerical examples are intentionally small; library documentation covers production implementations.</p><h2>Content & reuse</h2><p>Original ML Atlas lesson prose is licensed under CC BY 4.0. Attribute “ML Atlas” and link to this repository when reusing it. Code is MIT licensed. Linked publications, datasets, and documentation keep their own licenses. ML Atlas is not affiliated with or endorsed by the referenced authors.</p><h2>Privacy</h2><p>No learner account is required. Learning progress, notes, and bookmarks are stored in your browser on this device. Clearing site data removes them; exported files are under your control. The course does not add analytics or advertising trackers. The hosting service may process ordinary access logs.</p><p>Running Python loads the Pyodide runtime from a third-party CDN. Code runs in a browser worker; this app does not send your code to an execution server. External resource links and any services you choose to open have their own privacy terms.</p><h2>Corrections</h2><p>Found a mistake? <a className="text-link" href="https://github.com/SidduHG/Machine_learning/issues">Open a GitHub issue with the lesson and correction.</a></p></section></main>}
